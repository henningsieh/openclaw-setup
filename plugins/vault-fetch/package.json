{
  "name": "@openclaw-setup/vault-fetch",
  "version": "1.0.0",
  "description": "OpenClaw tool plugin exposing a vault_fetch agent tool backed by a self-hosted Vaultwarden vault via the Bitwarden CLI.",
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "plugin:build": "npm run build && openclaw plugins build --entry ./dist/index.js",
    "plugin:validate": "npm run build && openclaw plugins validate --entry ./dist/index.js",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "typebox": "^1.1.38"
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.17"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "openclaw": "2026.6.11",
    "typescript": "^5.6.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
