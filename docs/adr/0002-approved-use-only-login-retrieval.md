# Approved Use-Only Login Retrieval

The Vault Access Broker will return only a Vault Item's username-and-password pair, require explicit owner approval for every retrieval, and run only in an Interactive Verified-Owner Turn for Shelldon. Credential responses are for downstream login use rather than chat disclosure, and persisted tool results must be redacted; this accepts interaction friction to limit disclosure from the full Personal Vault Identity.
